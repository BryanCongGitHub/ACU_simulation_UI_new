[tool.black]
line-length = 88
target-version = ['py311']

[tool.pytest.ini_options]
# Do not force-load pytest-qt via -p; let pytest auto-discover plugins to avoid
# duplicate registration errors when plugin autoload is enabled.
testpaths = ["tests"]
